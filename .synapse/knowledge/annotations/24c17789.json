{
  "file": "_commands/Synapse/eager_dispatch.md",
  "content_hash": "645a5d36",
  "annotated_at": "2026-05-11T01:54:48Z",
  "annotated_by": "learn-bootstrap",
  "purpose": "Command specification for `!eager_dispatch`. It defines invocation syntax, resolution assumptions, dashboard/project context, required reads and writes, and success behavior for agents executing this command. Key content: **Purpose:** Run a standalone eager dispatch round on an active swarm. Reads current dashboard state, identifies all tasks whose dependencies are satisfied but haven't been dispatched, builds complete worker prompts, and dispatches them all immediately. The file is 54 lines long and should be treated as a knowledge/control-plane source rather than application code.",
  "exports": [
    {
      "name": "!eager_dispatch",
      "kind": "command",
      "signature": "!eager_dispatch [--dashboard {id}]"
    }
  ],
  "imports_from": {},
  "gotchas": [
    "Command files are executable specs: agents must read the whole file and follow it exactly; changing a command doc changes runtime behavior for future command invocations."
  ],
  "patterns": [
    "Command-spec pattern: title names the bang command, followed by syntax, phase/step procedures, required dashboard/project reads, and final reporting expectations.",
    "Uses markdown tables for compact command, file, or option matrices."
  ],
  "conventions": [
    "Markdown headings use a single H1 followed by H2/H3 sections for navigable documentation.",
    "Command names are written with leading `!` and placeholders use `{name}` braces for runtime paths or arguments.",
    "ASCII double hyphen is used in prose where many docs avoid Unicode punctuation for terminal readability."
  ],
  "relationships": {
    "depends_on": [],
    "depended_by": [],
    "related": []
  },
  "domains": [
    "command-system",
    "Synapse"
  ],
  "tags": [
    "markdown",
    "command-doc",
    "slash-command",
    "eager-dispatch",
    "swarm-command",
    "dashboard",
    "progress",
    "initialization-json"
  ],
  "complexity": "simple"
}
