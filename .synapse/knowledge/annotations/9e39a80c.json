{
  "file": "_commands/Synapse/p_synthesize.md",
  "content_hash": "f5d528a0",
  "annotated_at": "2026-05-11T01:54:48Z",
  "annotated_by": "learn-bootstrap",
  "purpose": "Command specification for `!p_synthesize`. It defines invocation syntax, resolution assumptions, dashboard/project context, required reads and writes, and success behavior for agents executing this command. Key content: > ## NON-NEGOTIABLE RULES \u2014 READ BEFORE ANYTHING ELSE > > **1. You are now the MASTER AGENT. You do NOT rewrite research artifacts directly. You do NOT modify any file under `documentation/research/{topic-slug}/` (the per-topic research is the immutable input). You ONLY plan, dispatch, and route worker outputs into `documentation/research/synthesis/`. Workers do all reading, deduplication, and stitching.** > > **2. You MUST read `{tracker_root}/a The file is 737 lines long and should be treated as a knowledge/control-plane source rather than application code.",
  "exports": [
    {
      "name": "!p_synthesize",
      "kind": "command",
      "signature": "!p_synthesize [topic-filter]"
    }
  ],
  "imports_from": {},
  "gotchas": [
    "Command files are executable specs: agents must read the whole file and follow it exactly; changing a command doc changes runtime behavior for future command invocations."
  ],
  "patterns": [
    "Command-spec pattern: title names the bang command, followed by syntax, phase/step procedures, required dashboard/project reads, and final reporting expectations.",
    "p_track-era planning pattern: plan.json carries shared context and task entries before initialization.json/progress files handle dashboard rendering and lifecycle state.",
    "Uses explicit NON-NEGOTIABLE language to encode hard behavioral constraints for agents.",
    "Uses fenced code blocks for command examples, schemas, or prompt templates."
  ],
  "conventions": [
    "Markdown headings use a single H1 followed by H2/H3 sections for navigable documentation.",
    "Command names are written with leading `!` and placeholders use `{name}` braces for runtime paths or arguments.",
    "ASCII double hyphen is used in prose where many docs avoid Unicode punctuation for terminal readability."
  ],
  "relationships": {
    "depends_on": [],
    "depended_by": [],
    "related": []
  },
  "domains": [
    "command-system",
    "Synapse"
  ],
  "tags": [
    "markdown",
    "command-doc",
    "slash-command",
    "p-synthesize",
    "swarm-command",
    "dashboard",
    "progress",
    "plan-json"
  ],
  "complexity": "complex"
}
