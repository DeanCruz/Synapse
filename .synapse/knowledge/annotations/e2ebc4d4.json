{
  "file": "_commands/project/learn_update.md",
  "content_hash": "fb65f1df",
  "annotated_at": "2026-05-11T01:54:48Z",
  "annotated_by": "learn-bootstrap",
  "purpose": "Command specification for `!learn_update`. It defines invocation syntax, resolution assumptions, dashboard/project context, required reads and writes, and success behavior for agents executing this command. Key content: **Purpose:** Incrementally refresh the Project Knowledge Index (PKI) by detecting stale annotations and re-scanning only changed files. This is the fast-path complement to `!learn` (full cold-start generation). It detects staleness via content hashes, the `stale` flag set by the PostToolUse hook, and new/deleted file discovery \u2014 then dispatches parallel agents only for files that actually need re-annotation. The file is 351 lines long and should be treated as a knowledge/control-plane source rather than application code.",
  "exports": [
    {
      "name": "!learn_update",
      "kind": "command",
      "signature": "!learn_update"
    }
  ],
  "imports_from": {},
  "gotchas": [
    "Command files are executable specs: agents must read the whole file and follow it exactly; changing a command doc changes runtime behavior for future command invocations."
  ],
  "patterns": [
    "Command-spec pattern: title names the bang command, followed by syntax, phase/step procedures, required dashboard/project reads, and final reporting expectations.",
    "p_track-era planning pattern: plan.json carries shared context and task entries before initialization.json/progress files handle dashboard rendering and lifecycle state.",
    "Uses fenced code blocks for command examples, schemas, or prompt templates."
  ],
  "conventions": [
    "Markdown headings use a single H1 followed by H2/H3 sections for navigable documentation.",
    "Command names are written with leading `!` and placeholders use `{name}` braces for runtime paths or arguments.",
    "ASCII double hyphen is used in prose where many docs avoid Unicode punctuation for terminal readability.",
    "Shell examples use fenced `bash` code blocks."
  ],
  "relationships": {
    "depends_on": [],
    "depended_by": [
      "documentation/project-integration/pki-overview.md"
    ],
    "related": []
  },
  "domains": [
    "command-system",
    "project"
  ],
  "tags": [
    "markdown",
    "command-doc",
    "slash-command",
    "learn-update",
    "project-command",
    "pki",
    "plan-json",
    "claude"
  ],
  "complexity": "complex"
}
