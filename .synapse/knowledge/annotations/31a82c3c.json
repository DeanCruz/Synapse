{
  "file": "_commands/project/scaffold.md",
  "content_hash": "531e1e08",
  "annotated_at": "2026-05-11T01:54:48Z",
  "annotated_by": "learn-bootstrap",
  "purpose": "Command specification for `!scaffold`. It defines invocation syntax, resolution assumptions, dashboard/project context, required reads and writes, and success behavior for agents executing this command. Key content: Scans the project's structure, detects its tech stack, reads its documentation, and generates a comprehensive `CLAUDE.md` file. Designed to bootstrap projects that lack agent instructions so the master agent and worker agents can operate effectively. The file is 165 lines long and should be treated as a knowledge/control-plane source rather than application code.",
  "exports": [
    {
      "name": "!scaffold",
      "kind": "command",
      "signature": "!scaffold \u2014 Generate CLAUDE.md for a Project"
    }
  ],
  "imports_from": {},
  "gotchas": [
    "Command files are executable specs: agents must read the whole file and follow it exactly; changing a command doc changes runtime behavior for future command invocations."
  ],
  "patterns": [
    "Command-spec pattern: title names the bang command, followed by syntax, phase/step procedures, required dashboard/project reads, and final reporting expectations.",
    "Uses fenced code blocks for command examples, schemas, or prompt templates.",
    "Uses markdown tables for compact command, file, or option matrices."
  ],
  "conventions": [
    "Markdown headings use a single H1 followed by H2/H3 sections for navigable documentation.",
    "Command names are written with leading `!` and placeholders use `{name}` braces for runtime paths or arguments.",
    "ASCII double hyphen is used in prose where many docs avoid Unicode punctuation for terminal readability.",
    "Shell examples use fenced `bash` code blocks."
  ],
  "relationships": {
    "depends_on": [],
    "depended_by": [],
    "related": []
  },
  "domains": [
    "command-system",
    "project"
  ],
  "tags": [
    "markdown",
    "command-doc",
    "slash-command",
    "scaffold",
    "project-command",
    "claude",
    "sse"
  ],
  "complexity": "moderate"
}
