{
  "file": ".claude/plan.md",
  "content_hash": "fc40953c",
  "annotated_at": "2026-05-11T01:54:48Z",
  "annotated_by": "learn-bootstrap",
  "purpose": "Control-plane markdown document: Plan: Convert XML Task Files to JSON. Key content: Replace the XML-based master task file format (`parallel_{name}.xml`) with JSON (`parallel_{name}.json`) across the entire Synapse codebase. This affects ~50 documentation/instruction files and zero application code (no programmatic XML parsing exists). The file is 181 lines long and should be treated as a knowledge/control-plane source rather than application code.",
  "exports": [],
  "imports_from": {},
  "gotchas": [
    "Legacy task-record reference: this file mentions tasks/{date}/parallel_* artifacts; current p_track planning context is canonical in dashboards/{dashboardId}/plan.json, with lifecycle state in progress files."
  ],
  "patterns": [
    "Control-plane documentation pattern: markdown is treated as operational configuration for agents, not just explanatory prose.",
    "Uses fenced code blocks for command examples, schemas, or prompt templates."
  ],
  "conventions": [
    "Markdown headings use a single H1 followed by H2/H3 sections for navigable documentation.",
    "Agent-control documents use `{tracker_root}`, `{project_root}`, and `{dashboardId}` placeholders instead of machine-specific paths.",
    "ASCII double hyphen is used in prose where many docs avoid Unicode punctuation for terminal readability."
  ],
  "relationships": {
    "depends_on": [],
    "depended_by": [],
    "related": [
      ".claude/README.md"
    ]
  },
  "domains": [
    "control-plane-docs"
  ],
  "tags": [
    "markdown",
    "control-plane",
    "dashboard",
    "progress",
    "initialization-json",
    "claude",
    "electron",
    "ide"
  ],
  "complexity": "moderate"
}
