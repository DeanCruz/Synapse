{
  "file": "_commands/project/product_plan.md",
  "content_hash": "78879145",
  "annotated_at": "2026-05-11T01:54:48Z",
  "annotated_by": "learn-bootstrap",
  "purpose": "Command specification for `!product_plan`. It defines invocation syntax, resolution assumptions, dashboard/project context, required reads and writes, and success behavior for agents executing this command. Key content: Take a **loose product spec prompt** and produce a single, exhaustive **`{product_name}_spec_sheet.md`** that captures every dimension a builder needs to make a confident, informed decision about how to construct the product. The spec sheet is the user's deliverable. It is built from parallel deep research, then synthesized into one document. The file is 784 lines long and should be treated as a knowledge/control-plane source rather than application code.",
  "exports": [
    {
      "name": "!product_plan",
      "kind": "command",
      "signature": "!product_plan {prompt}"
    }
  ],
  "imports_from": {},
  "gotchas": [
    "Command files are executable specs: agents must read the whole file and follow it exactly; changing a command doc changes runtime behavior for future command invocations."
  ],
  "patterns": [
    "Command-spec pattern: title names the bang command, followed by syntax, phase/step procedures, required dashboard/project reads, and final reporting expectations.",
    "Uses explicit NON-NEGOTIABLE language to encode hard behavioral constraints for agents.",
    "Uses fenced code blocks for command examples, schemas, or prompt templates.",
    "Uses markdown tables for compact command, file, or option matrices."
  ],
  "conventions": [
    "Markdown headings use a single H1 followed by H2/H3 sections for navigable documentation.",
    "Command names are written with leading `!` and placeholders use `{name}` braces for runtime paths or arguments.",
    "ASCII double hyphen is used in prose where many docs avoid Unicode punctuation for terminal readability."
  ],
  "relationships": {
    "depends_on": [],
    "depended_by": [],
    "related": []
  },
  "domains": [
    "command-system",
    "project"
  ],
  "tags": [
    "markdown",
    "command-doc",
    "slash-command",
    "product-plan",
    "project-command",
    "pki",
    "progress",
    "profile"
  ],
  "complexity": "complex"
}
