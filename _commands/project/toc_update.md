# Legacy index refresh command

This legacy command is deprecated. Execute `!learn_update` instead.

## Required Behavior

1. Read `{tracker_root}/_commands/project/learn_update.md` in full.
2. Execute it exactly as if the user had typed `!learn_update`.
3. In the final response, state that the project knowledge graph was refreshed under `{project_root}/.synapse/knowledge/`.

Do not create or update the legacy markdown index. The project knowledge graph lives under `{project_root}/.synapse/knowledge/`.
