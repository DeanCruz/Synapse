# Legacy index generation command

This legacy command is deprecated. Execute `!learn` instead.

## Required Behavior

1. Read `{tracker_root}/_commands/project/learn.md` in full.
2. Execute it exactly as if the user had typed `!learn`.
3. In the final response, state that the project knowledge graph now lives under `{project_root}/.synapse/knowledge/`.

Do not create or update the legacy markdown index. The project knowledge graph lives under `{project_root}/.synapse/knowledge/`.
